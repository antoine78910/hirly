{
  "name": "hirly",
  "private": true,
  "packageManager": "bun@1.3.14",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "format": "bun run --workspaces --if-present format",
    "lint": "bun run --workspaces --if-present lint",
    "typecheck": "bun run --workspaces --if-present typecheck",
    "test": "bun test ./tests/workspace-isolation.test.ts ./tests/g002-contracts.test.ts ./tests/g002-database-foundation.test.ts ./tests/g002-postgres.integration.test.ts && bun run --workspaces --if-present test",
    "build": "bun run --workspaces --if-present build"
  },
  "devDependencies": {
    "@types/bun": "1.3.14",
    "typescript": "5.9.3"
  }
}
