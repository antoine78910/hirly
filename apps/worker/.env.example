NODE_ENV=development
JOBS_DATABASE_URL=postgresql://worker:change-me@localhost:5432/hirly?sslmode=require
PORT=3001
WORKER_CONCURRENCY=4
WORKER_LEASE_SECONDS=120
WORKER_HEARTBEAT_SECONDS=30
WORKER_POLL_MS=1000
WORKER_SCHEDULE_POLL_MS=5000
WORKER_SHUTDOWN_MS=25000
WORKER_CONTROL_ENABLED=false
JOB_PROJECTION_ENABLED=false
PROJECTION_RECONCILIATION_ENABLED=false
JOB_PROJECTION_BATCH_SIZE=10
JOB_PROJECTION_RECONCILIATION_BATCH_SIZE=100
# WORKER_CONTROL_TOKEN must contain at least 32 characters when enabled.
