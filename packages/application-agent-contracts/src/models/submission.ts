import { z } from 'zod'; import { isoTimestamp, opaqueId, origin, safeReasonCode, safeRef, sha256, version } from './common';
export const ApplicationSubmissionPlanSchema = z.object({ id: opaqueId('submission_plan'), version: z.number().int().positive(), draftId: opaqueId('draft'), candidateEvidenceSnapshotId: opaqueId('evidence_snapshot'), jobSnapshotId: opaqueId('job_snapshot'), candidateSnapshotFingerprint: sha256, jobSnapshotFingerprint: sha256, draftFingerprint: sha256, secureFormPayloadRef: safeRef, formPayloadFingerprint: sha256, targetOrigin: origin, adapterKey: z.string().regex(/^[a-z][a-z0-9-]{1,63}$/), adapterVersion: version, allowedActionIds: z.array(z.string().regex(/^[a-z][a-z0-9-]{1,63}$/)).min(1), idempotencyKey: z.string().regex(/^idem_[a-zA-Z0-9_-]{8,128}$/), expiresAt: isoTimestamp, planDigest: sha256 }).strict();
export const ApplicationSubmissionReceiptSchema = z.object({ attemptId: opaqueId('attempt'), planId: opaqueId('submission_plan'), planDigest: sha256, approvalReceiptId: opaqueId('approval'), idempotencyKey: z.string().regex(/^idem_[a-zA-Z0-9_-]{8,128}$/), targetOrigin: origin, adapterKey: z.string().regex(/^[a-z][a-z0-9-]{1,63}$/), adapterVersion: version, status: z.enum(['dry_run_completed','submitted','blocked','failed','unconfirmed']), providerApplicationId: z.string().regex(/^provider:[a-zA-Z0-9._-]{3,128}$/).optional(), safeEvidenceRefs: z.array(safeRef), observedAt: isoTimestamp, reasonCodes: z.array(safeReasonCode).optional() }).strict();
export type ApplicationSubmissionPlan=z.infer<typeof ApplicationSubmissionPlanSchema>; export type ApplicationSubmissionReceipt=z.infer<typeof ApplicationSubmissionReceiptSchema>;
